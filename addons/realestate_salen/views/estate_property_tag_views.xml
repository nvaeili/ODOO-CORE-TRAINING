<odoo>
    <!-- Action window for managing Property Tags -->
    <record id="estate_property_tag_action" model="ir.actions.act_window">
        <field name="name">Property Tags</field>
        <field name="res_model">estate_property_tag</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!-- Tree view for Property Tags -->
    <record id="estate_property_tag_view_tree" model="ir.ui.view">
        <field name="name">estate_property_tag.tree</field>
        <field name="model">estate_property_tag</field>
        <field name="arch" type="xml">
            <!-- Displaying Property Tags in a tree structure -->
            <tree string="Property Tags" editable="bottom">
                <field name="name"/>
            </tree>
        </field>
    </record>
</odoo>
